# DayMate 개발 및 운영 원칙

이 문서는 DayMate 프로젝트에서 AI가 반드시 지켜야 할 개발·검증·배포 원칙이다.

새로운 대화를 시작하거나 다른 AI가 프로젝트를 맡더라도 작업 전에 이 문서를 먼저 읽고 준수한다.

---

## 1. 기본 작업 원칙

- 요청받은 범위 안에서만 작업한다.
- 요청과 관계없는 코드나 이미 정상 작동하는 기능은 임의로 수정하지 않는다.
- 여러 항목을 순차적으로 요청받은 경우 항목마다 사용자에게 확인받지 말고 계속 진행한다.
- 구현·수정·테스트·Git 커밋·push까지는 별도 승인 없이 진행할 수 있다.
- 작업이 끝나면 변경 내용, 검증 결과, 주의사항을 한 번에 요약해서 보고한다.
- 실제 프로덕션 배포 직전에만 사용자에게 최종 승인을 받는다.
- 외부 API 등록, 유료 서비스 연결, 과금 발생, 개인정보 관련 변경, 데이터 삭제 등은 실행 전에 반드시 사용자 승인을 받는다.
- 사용자가 작업 도중 새로운 조건을 제시하면 기존 작업에 함께 반영한다.
- 사용자가 직접 수정한 내용이나 작업 범위 밖의 기존 변경사항을 임의로 되돌리지 않는다.

---

## 2. 작업 진행 순서

기능 개발이나 오류 수정은 다음 순서로 진행한다.

1. 사용자의 요청과 완료 조건을 정리한다.
2. 관련 코드와 기존 기능을 먼저 확인한다.
3. 변경 범위와 발생 가능한 영향을 점검한다.
4. 요청 범위 안에서 코드를 수정한다.
5. 빌드 및 가능한 테스트를 실행한다.
6. 기존 기능에 문제가 생기지 않았는지 확인한다.
7. 변경된 파일과 내용을 검토한다.
8. Git commit 및 push를 진행한다.
9. 변경 내용과 검증 결과를 사용자에게 요약한다.
10. 프로덕션 배포 승인을 받는다.
11. 승인 후 Vercel 프로덕션 배포를 실행한다.
12. 실제 서비스 반영 여부를 직접 확인한다.

사소한 문구·색상·간격 수정은 간단하게 처리할 수 있지만, 다음 작업은 영향 범위를 더 엄격하게 검토한다.

- Firestore 데이터 구조 변경
- 로그인 및 사용자 인증
- 사용자별 데이터 접근 권한
- 개인정보 또는 사진 저장
- 서비스워커와 캐시
- 외부 API 연결
- cron 설정
- 프로덕션 배포
- 기존 데이터 삭제 또는 마이그레이션

---

## 3. 필수 배포 절차

프로덕션 배포는 반드시 다음 순서를 지킨다.

### 3.1 버전 자동 갱신

배포 전에 다음 명령어를 실행한다.

```bash
npm run generate:version
```

이 명령으로 변경되는 다음 파일은 절대로 되돌리지 않는다.

- `package.json`
- `src/version.js`
- `public/sw.js`

생성된 버전 변경분은 반드시 Git 커밋에 포함한다.

### 3.2 배포 전 확인

다음 사항을 확인한다.

- 빌드가 정상적으로 완료되는가
- 변경 파일에 토큰, 비밀번호, 개인정보가 포함되지 않았는가
- 서비스워커와 버전 파일이 정상적으로 갱신되었는가
- Firestore 구조 변경 시 보안 규칙도 함께 준비되었는가
- Vercel Hobby 플랜의 cron 제한을 위반하지 않는가
- `.vercelignore`에 `node_modules`, `desktop`, `.git`, `dist`가 포함되어 있는가 (빠지면 Vercel 100MB 파일 크기 제한으로 배포 자체가 실패한다)
- 기존 기능에 명백한 오류가 발생하지 않는가

### 3.3 Git 저장

버전 변경분과 작업 내용을 함께 커밋한 후 다음 순서로 push한다.

```bash
git commit
git push origin main
```

Git commit과 push는 작업 결과를 저장하는 단계이며 현재는 프로덕션 배포가 아니다.

현재 DayMate는 GitHub와 Vercel의 자동배포가 연결되어 있지 않기 때문에 `main`에 push하는 것만으로는 서비스가 배포되지 않는다.

향후 GitHub-Vercel 자동배포를 연결하면 이 규칙을 즉시 수정해야 한다.

### 3.4 사용자 승인

Git push까지 완료한 후 사용자에게 다음 내용을 요약한다.

- 구현하거나 수정한 기능
- 주요 변경 파일
- 테스트 및 빌드 결과
- Firestore 보안 규칙 변경 필요 여부
- cron 관련 확인 결과
- 예상되는 영향과 주의사항
- 배포 후 확인할 내용

사용자의 승인을 받은 후에만 실제 프로덕션 배포를 실행한다.

### 3.5 Vercel 수동 배포

사용자 승인 후 다음 명령으로 프로덕션 배포를 실행한다.

```bash
vercel deploy --prod --yes --token "$TOKEN"
```

`$TOKEN`은 `.env.local`에서 로드한다 (`.env.local`은 gitignore 대상). 토큰이 만료되었으면 Vercel 대시보드 Settings → Tokens에서 재발급한다 (No Expiration으로 생성).

주의사항:

- `$TOKEN` 값을 코드, 문서, 로그, 채팅 또는 Git 저장소에 노출하지 않는다.
- `.env.local` 전체를 셸 환경변수로 불러오지 않는다(`source`/`set -a` 금지). 이 파일에 `VERCEL_PROJECT_ID`만 있고 `VERCEL_ORG_ID`가 없어서, 통째로 불러오면 `You specified VERCEL_PROJECT_ID but you forgot to specify VERCEL_ORG_ID` 오류로 배포가 실패한다. 토큰 값만 읽어서 `--token`으로 넘긴다 (2026-09-23 발생).
- Git push만으로 배포되었다고 판단하지 않는다.
- Vercel 명령의 성공 결과와 실제 서비스 반영 여부를 모두 확인한다.

### 3.6 실제 반영 확인

배포가 끝나면 `curl` 등을 사용하여 실제 운영 주소의 버전과 응답을 확인한다.

브라우저 화면만 보고 배포 성공 여부를 판단하지 않는다. 브라우저 캐시와 서비스워커 때문에 이전 버전이 표시될 수 있다.

필요하면 캐시 무효화용 쿼리 값을 추가해 실제 응답을 확인한다.

확인할 내용:

- 실제 서비스 접속 여부
- 최신 버전 반영 여부
- 수정한 기능의 정상 작동 여부
- 서비스워커 업데이트 여부
- 주요 화면의 오류 여부
- 브라우저 콘솔 또는 네트워크 오류 여부

배포 명령이 성공했더라도 실제 서비스 확인이 끝나기 전까지 배포 완료로 판단하지 않는다.

---

## 4. Vercel cron 주의사항

DayMate는 Vercel Hobby 플랜을 사용한다.

Hobby 플랜에서 하루 1회를 초과하는 cron 스케줄이 하나라도 존재하면 이후 모든 배포가 오류 메시지 없이 조용히 실패할 수 있다.

따라서 다음 원칙을 반드시 지킨다.

- 배포 전에 `vercel.json` 등 cron 설정을 확인한다.
- 하루 1회를 초과하는 cron 스케줄을 만들지 않는다.
- cron 설정을 추가하거나 변경할 때는 Hobby 플랜 제한을 먼저 확인한다.
- 배포가 이유 없이 반영되지 않으면 cron 제한 위반 여부를 우선 점검한다.
- cron과 관련된 변경사항은 배포 전 사용자에게 별도로 알린다.

---

## 5. Firestore 개발 원칙

Firestore에 새로운 컬렉션이나 서브컬렉션을 추가할 때는 Firebase 보안 규칙에도 해당 경로의 `match` 블록을 반드시 추가한다.

이 프로젝트에는 `firestore.rules` 파일이 로컬 저장소에 없다 — 보안 규칙은 Firebase 콘솔(Firestore Database → 규칙)에서만 관리된다. 상위 경로에 규칙이 있어도 하위 서브컬렉션에는 자동 상속되지 않으므로, 서브컬렉션마다 명시적으로 `match` 블록이 있어야 한다.

보안 규칙을 추가하지 않으면 코드와 화면은 정상적으로 보여도 실제 저장·조회 과정에서 `permission-denied` 오류가 발생할 수 있다. "저장/등록이 안 된다"는 제보를 받으면 먼저 브라우저 콘솔 에러부터 확인한다 — "Missing or insufficient permissions"가 보이면 규칙 문제로 바로 확정할 수 있다.

Firestore 구조 변경 시 다음을 함께 확인한다.

- 컬렉션 및 서브컬렉션 경로
- 문서 구조와 필드
- 사용자별 읽기·쓰기 권한
- Firebase 보안 규칙의 대응 `match` 블록
- 기존 데이터와의 호환성
- 데이터 삭제 또는 덮어쓰기 가능성
- Firebase 콘솔에서 별도로 적용해야 할 내용

규칙 수정 자체는 Codex가 로컬에서 할 수 없다 — 기존 비슷한 컬렉션(예: `notices/comments`)의 규칙 구조를 사용자에게 물어서, 그 패턴을 미러링한 전체 교체본을 만들어 사용자가 Firebase 콘솔에 붙여넣고 게시(Publish)하도록 안내한다.

보안 규칙 반영이 필요한 상태에서 코드만 배포하고 작업이 완전히 끝났다고 보고하지 않는다.

### 커뮤니티·게시판 권한 원칙 (2026-09-24 사용자 확정)

- **수정·삭제 권한은 같다:** 글쓴이 본인 + 관리자만 가능하다. 커뮤니티 안의 글은 커뮤니티 관리자(`communities/{id}.createdBy`)와 앱 관리자(`admin/config.uids`)가 관리자이다. 자유게시판(`publicBoard`)은 앱 관리자만 관리자이다.
- 다른 사람은 **앱 기능상 꼭 필요한 필드만** 바꿀 수 있다: 게시글·공지의 `commentCount`(댓글 달기·지우기), 댓글의 `likedBy`(좋아요), 커뮤니티 문서의 `memberCount`·`lastActivityAt`.
- 작성은 **본인 명의로만** 가능하다(`uid`/`createdBy`가 내 uid, 멤버·출석 문서 id가 내 uid). 커뮤니티 관리자(`createdBy`) 변경은 현재 관리자나 앱 관리자만 할 수 있다.
- 커뮤니티 자체 삭제는 앱 관리자만 가능하다(관리자 화면). 사용자가 이 상태를 유지하기로 했다.
- 새 기능이 남의 문서를 수정해야 하면(예: 조회수, 반응) 규칙의 허용 필드 목록(`onlyChanges([...])`)에 추가해야 한다. 안 하면 조용히 permission-denied가 난다.
- 챌린지(`challenges`)에는 이 원칙이 아직 적용되지 않았다.

### 보안 규칙 변경 시 검증 방법

- 규칙은 Firestore 에뮬레이터로 먼저 검증한다. 스크래치패드 같은 프로젝트 밖 임시 폴더에 `firebase-tools@13` + `@firebase/rules-unit-testing` + `firebase`를 설치하고, `npx firebase emulators:exec --only firestore --project demo-daymate "node test.mjs"`로 실행한다. `demo-` 프로젝트라 실제 Firebase에는 접속하지 않는다.
- Java는 `C:\baduk\LizzieYZY\jre\java11\bin`의 Java 11을 PATH에 넣어서 쓴다. firebase-tools 14 이상은 더 높은 Java가 필요하므로 13을 쓴다.
- "막혀야 할 것"과 "앱이 실제로 보내는 요청과 같은 형태의 허용 케이스"를 둘 다 테스트한다.
- 게시 후 실제 확인은 **운영자 계정이 아닌 일반 계정**으로 한다. 운영자는 앱 관리자 조건으로 모든 규칙을 통과하므로 확인이 되지 않는다.

### 로컬 전용 값을 계정 동기화로 옮길 때

localStorage에만 있던 설정값을 `users/{uid}/data/settings` 동기화 대상으로 옮길 때는, "값이 바뀌면 저장"하는 useEffect만으로는 부족하다. 기존 사용자가 이미 입력해 둔 값은 다시 수정하지 않는 한 영원히 서버에 올라가지 않는다.

- 로그인 로드 시 **서버에 필드가 없고 로컬에만 값이 있으면 업로드**하는 1회성 이관 로직을 함께 넣는다 (예: `App.jsx`의 birthDate 처리).
- 함께 쓰는 필드(예: 생년월일+출생시간)는 묶어서 올린다. 먼저 열린 기기의 값이 기준이 되므로, 기기마다 값이 달랐다면 사용자가 한쪽에서 다시 입력해야 맞춰진다.
- settings 문서는 실시간 리스너가 아니라 **로그인(앱 시작) 시** 불러온다. 앱 복귀 시에는 **언젠가할일·인생목표·실천항목·목표(goals 문서)만** 다시 받는다(`applyRemotePrefs`). 그 밖의 설정값은 다른 기기에서 바꾸면 앱을 완전히 닫았다 열어야 반영된다는 점을 사용자에게 안내한다.
- 데스크탑 앱끼리는 같은 브라우저 저장소를 공유하므로, 데스크탑 간에 값이 맞는다고 해서 계정 동기화가 되는 것으로 판단하지 않는다. 동기화 검증은 휴대폰 등 다른 기기로 한다.
(2026-09-23, v559 생년월일시 동기화가 휴대폰에 반영되지 않던 문제에서 확정)

---

## 6. 사진 및 실물 문서 처리 원칙

명함, 신분증, 영수증 등 사용자가 가진 실물 문서를 저장하는 기능은 앱에서 새롭게 디자인하거나 임의로 재구성하지 않는다.

기본 원칙은 사용자가 촬영하거나 보유한 사진 원본을 그대로 저장하고 보여주는 것이다. (예: "명함 저장" 요청을 입력폼 + html2canvas로 새로 디자인해서 만들었다가, 이미 가진 명함 사진 한 장을 그대로 저장/전송하면 되는 것으로 정정된 전례가 있다.)

다음 사항을 지킨다.

- 사진을 임의로 카드나 그래픽 형태로 재디자인하지 않는다.
- 원본의 비율과 내용을 가능한 한 유지한다.
- 자동 자르기, 보정, OCR 변환 등은 사용자가 요청한 경우에만 적용한다.
- 사진 원본이 사라지거나 덮어써지지 않도록 주의한다.
- 개인정보가 포함된 사진은 외부 서비스로 임의 전송하지 않는다.
- 애매하면 구현 전에 "앱이 새로 만들지, 원본을 그대로 다룰지"를 먼저 물어본다 — 애매할 때는 원본 그대로가 기본값이다.

---

## 7. 사진 첨부 입력 원칙

사진 첨부용 HTML `input`에는 `capture` 속성을 넣지 않는다.

```html
<input type="file" accept="image/*">
```

다음과 같이 `capture` 속성을 추가하지 않는다.

```html
<input type="file" accept="image/*" capture>
```

DayMate에서는 카메라 강제 실행보다 사용자가 사진 앨범에서 기존 사진을 선택하는 흐름을 우선한다. (카메라 우선으로 하면 기종에 따라 카메라 앱에 앨범 바로가기가 없어 앨범 접근 자체가 막히는 문제가 있었다.)

`PhotoAttach.jsx`, `PhotoGallery.jsx`, `LongMemoEditor.jsx`의 파일 input이 이 원칙을 따르고 있으며, 사진 관련 새 컴포넌트를 만들 때도 동일한 패턴(속성 없음)을 따른다.

이 결정은 최종 확정된 기본 원칙이다. 사용자가 명시적으로 변경을 요청하지 않는 한 임의로 바꾸지 않는다.

---

## 8. 글씨 및 화면 크기 원칙

사용자는 큰 글씨와 보기 편한 화면을 선호한다.

현재 DayMate에는 `px` 단위의 인라인 스타일이 많기 때문에 전체적인 글씨와 레이아웃 크기를 조정할 때는 개별 `font-size`를 대량으로 수정하지 않고 CSS `zoom`을 우선 사용한다. (`font-size` 변경은 `em`/`rem` 단위에만 적용되고 `px` 고정값 인라인 스타일에는 무효하지만, `zoom`은 레이아웃·이미지·인라인 스타일 전체에 적용된다.)

다음 원칙을 지킨다.

- 전체 크기 조절은 CSS `zoom`을 우선 검토한다.
- 기존 레이아웃의 비율을 유지한다.
- 글씨만 커지고 버튼이나 입력창이 작아지는 불균형을 피한다.
- 모바일 화면에서 잘림이나 가로 스크롤이 발생하지 않는지 확인한다.
- 큰 글씨에서도 주요 버튼과 입력 영역이 정상적으로 보이는지 확인한다.
- 사용자의 명시적인 요청 없이 기존 크기 체계를 전면 변경하지 않는다.

---

## 9. 기존 기능 보호 원칙

이미 정상적으로 완성된 기능은 새로운 작업과 직접 관련되지 않는 한 수정하지 않는다.

수정이 불가피하면 다음 내용을 먼저 확인한다.

- 왜 기존 코드를 수정해야 하는가
- 수정하지 않고 구현할 다른 방법은 없는가
- 어떤 기존 기능에 영향을 줄 수 있는가
- 문제가 발생하면 어떻게 되돌릴 것인가
- 수정 후 어떤 방법으로 기존 기능을 확인할 것인가

핵심 기능을 변경할 때는 기존 동작을 보존하는 테스트나 검증 절차를 함께 마련한다.

"코드를 수정하지 말라"는 문구에만 의존하지 않고, 빌드·기능 테스트·실제 실행 확인을 통해 기존 기능이 유지되는지 검증한다.

---

## 10. 오류 및 재발 방지 기록

작업 중 중요한 오류가 발생하면 다음 내용을 기록한다.

- 발생한 현상
- 실제 원인
- 해결 방법
- 재발 방지 방법
- 다음 작업에서 반드시 확인할 사항

모든 사소한 시행착오를 기록하지는 않는다.

다음 작업에서 다시 발생할 가능성이 있거나 프로젝트 운영에 영향을 주는 내용만 기록한다.

특히 다음 내용은 반드시 기록한다.

- 배포 실패 원인
- Firestore 권한 오류
- 데이터 손상 또는 유실 위험
- 서비스워커 및 캐시 문제
- cron 제한 문제
- 외부 API 설정 문제
- 여러 번 반복된 오류
- 사용자가 확정한 중요한 디자인·기능 원칙

---

## 11. "저장해" 명령의 의미

사용자가 "저장해"라고 말하면 그날 작업 내용을 단순히 대화 메모리에만 저장하지 않는다.

그날 작업 중 앞으로의 개발에 영향을 주는 다음 내용을 정리한다.

- 새로 확정된 개발 원칙
- 중요한 기능 결정
- 사용자의 디자인 및 사용성 선호
- 오류의 원인과 해결 방법
- 재발 방지 대책
- 배포 및 운영상 주의사항
- 다음 작업에서 이어서 해야 할 내용

단순 작업 내역이나 임시 시행착오는 제외한다.

새로 확정된 원칙이나 정책성 결정은 이 `AGENTS.md` 문서 자체를 갱신해서 반영한다. 그 외 최근 작업 맥락(무엇을 왜 했는지, 다음에 이어할 일 등)은 AI 메모리(`project_today` 등)에 저장한다.

이 문서와 AI 메모리의 내용이 다르면 이 `AGENTS.md`를 우선한다.

---

## 12. 작업 완료 보고 형식

작업이 끝나면 다음 형식으로 간결하게 보고한다.

### 작업 내용

- 구현하거나 수정한 기능

### 변경 파일

- 주요 변경 파일과 변경 이유

### 검증 결과

- 빌드 결과
- 테스트 결과
- 직접 확인한 내용

### 추가 조치

- Firebase 콘솔 설정
- 외부 API 등록
- 사용자 확인 필요 사항
- 프로덕션 배포 승인 필요 여부

### 주의사항

- 예상되는 영향
- 캐시 또는 서비스워커 관련 사항
- 되돌리는 방법
- 아직 확인하지 못한 내용

검증하지 않은 내용을 검증했다고 보고하지 않는다. 오류가 있거나 확인하지 못한 부분이 있으면 숨기지 말고 명확하게 알린다.

---

## 13. 우선순위

규칙이 서로 충돌할 경우 다음 우선순위를 적용한다.

1. 사용자가 현재 대화에서 직접 내린 최신 지시
2. 이 문서의 개발 및 배포 원칙
3. AI 메모리에 기록된 과거 결정
4. 기존 코드에서 확인되는 관례
5. AI의 일반적인 판단

사용자의 최신 지시가 기존 규칙을 변경하는 경우, 작업이 끝난 후 이 문서도 최신 상태로 갱신한다.

---

## 14. 화면·기능 구조 원칙 (2026-09-24 사용자 확정)

### 14.1 목록은 보기, 편집은 상세 창

- 할일 목록(오늘 탭·달력 미리보기)은 **보기 전용**이다. 목록에서 바로 누르는 것은 **체크, "언젠가"(미완료만), ✕ 삭제(확인창)**뿐이다. ✕는 언젠가할일 목록과 같은 회색 ✕ 모양을 쓴다.
- 제목·시간·성장 스탯·메모·사진·드라이브 파일·삭제 같은 편집은 줄을 눌러 여는 **할일 상세 창(`TaskDetailSheet`)**에서 한다.
- **할일과 언젠가할일은 완전히 같은 상세 창을 쓴다.** 개발 편의를 위해 화면을 나누지 않는다. 언젠가할일에서 시간이나 스탯이 의미가 적어도 칸을 숨기지 않는다.
- 상세 창에 "언젠가로 보내기" 버튼은 두지 않는다. 목록에 이미 있다.
- 날짜 상세 화면(자세히보기)은 제목을 바로 입력하는 편집 화면이라 예외로, "📝 상세" 버튼으로 창을 연다.
- 달력 미리보기에는 하루 메모 칸을 두지 않는다. 일정별 메모는 할일 상세, 하루 메모는 자세히보기에서 다룬다.

### 14.2 할일 ↔ 언젠가 이동 시 상세 정보는 한 곳에서

- 할일 ↔ 언젠가할일로 옮길 때 함께 가져가는 필드(메모·사진·파일·시간·스탯·목표연결)는 **`src/utils/taskDetail.js`의 `pickTaskDetail`** 한 곳에서 정의한다.
- 상세 창에 새 항목을 추가하면 **여기에도 추가**해야 이동할 때 사라지지 않는다.

### 14.3 목표 → 실천 항목 → 언젠가 → 오늘

- 인생목표·올해 목표를 누르면 **목표 상세 창(`GoalDetailSheet`, 두 목표 공통)**이 열린다. "이루기 위해 할 것들"을 적고, 항목마다 **"언젠가로"** 버튼으로 언젠가할일을 만든다. 오늘 할일로 바로 보내는 버튼은 두지 않는다. 할일이 생기는 길은 목표 → 언젠가 → 오늘 한 줄이다.
- 파생된 할일에는 `goalRef({kind, title})`가 붙어 🎯 표시가 나고, 오늘로 옮겨도 유지된다.
- 올해 목표는 기존 `goals.year[].actions`(달력 목표 패널의 액션플랜)를 쓴다. 인생목표는 `lifeGoals`(문자열 목록)를 건드리지 않고 **`lifeGoalActions` 배열**에 따로 저장한다.
- 목표 목록을 편집·저장할 때는 **`matchGoalsByTitle`**로 기존 항목을 이어받는다. 제목 문자열만 넘기면 실천 항목이 지워진다. 실제로 My 탭 올해 목표 편집이 액션플랜을 지우던 버그가 있었다.
- 인생목표와 올해 목표는 서로 연결하지 않는다. 월간 목표는 이 구조에서 제외한다.

### 14.4 파일 첨부는 구글 드라이브

- 할일·긴 메모의 파일은 사용자 구글 드라이브 **"DayMate 첨부파일"** 폴더에 올리고, 앱에는 `{id, name, link, mimeType, size}`만 저장한다(`DriveFiles.jsx`). 기존 드라이브 연결(`drive.file` 권한, 이 앱이 만든 파일만 접근)을 재사용한다.
- 목록에서 ✕를 누르거나 할일을 삭제해도 **드라이브 파일은 지우지 않는다**(링크만 뺀다). 사용자가 정한 것이다.
- 드라이브 연결이 만료됐으면 첫 번째 누름은 연결만 하고, 두 번째 누름에 파일을 고르게 한다(연결 직후 파일 선택 창을 열면 팝업 차단에 걸린다).
- 회사 업무 파일은 올리지 않도록 권장한다(금융회사 외부 반출 이슈).
- 사진(청첩장 등)은 기존 Firebase Storage `users/{uid}/memos` 경로를 쓴다.

### 14.5 휴대폰 연락처 가져오기 (내 사람들)

- 안드로이드 크롬의 Contact Picker(`navigator.contacts.select`)만 쓴다. 삼성 인터넷·아이폰은 지원하지 않고, 아이폰은 추후 vCard(.vcf) 파일 가져오기로 대응한다.
- 개인정보 원칙: 사용자가 연락처 창에서 **직접 고른 사람만** 넘어오고, **내 계정에만** 저장하며, 외부로 보내지 않는다. 요청 항목은 이름·전화만이다(이메일까지 요청하면 느리다).
- 흐름: 안내 창 → 안드로이드 창에서 **모두 선택 → 확인** → DayMate 목록에서 검색하며 체크. 안드로이드 창 안에서 검색 후 엔터를 누르면 선택 없이 닫히므로 그 창에서는 검색하지 않게 안내한다.
- 크롬의 "연락처 접근 허용" 창은 웹이 강제로 띄울 수 없고, 한 번 거부하면 다시 뜨지 않는다. 빈 결과가 오면 **설정 → 애플리케이션 → Chrome → 권한 → 연락처** 안내 창을 보여준다.
- 이미 등록된 사람(같은 전화번호, +82 ↔ 0 동일 취급)은 건너뛰고 덮어쓰지 않는다.

---

## 15. 기술 재발 방지 메모

- **시트·팝업 창은 `.dm-phone`에 포털로 렌더링한다.** 화면 본문 `S.content`가 `zIndex:1` 쌓임 맥락이라, 그 안에 그리면 z-index를 아무리 높여도 하단 네비(`zIndex:100`)에 가려진다. `TaskDetailSheet`·`GoalDetailSheet`·`ContactImportSheet`처럼 `createPortal(…, document.querySelector('.dm-phone'))`를 쓴다. 큰글씨 zoom도 `.dm-phone` 범위라 그대로 적용된다.
- **버튼 크기를 지정할 때는 `padding: 0`을 명시한다.** `index.css` 전역 `button`에 좌우 padding 1.2em이 있어서, 32px로 지정한 버튼도 실제로는 80px 가까이 넓어진다. 오늘의 할일 헤더가 두 줄로 깨졌던 원인이다.
- **`useState`는 조기 return보다 위에 선언한다.** Today.jsx처럼 `if (showSearch) return …`이 있는 컴포넌트에서 아래쪽에 state를 추가하면 React 규칙 위반이다.
- **`saveSettings`는 `merge: true`다.** 목록을 객체(맵)로 저장하면 지운 키가 서버에 남는다. 목표 제목 같은 이름을 키로 쓰는 목록은 **배열**로 저장한다.
- **한글 검색은 NFC 정규화를 한다.** 휴대폰에서 가져온 이름이 자모 분리(NFD)로 들어오면 겉보기엔 같아도 검색되지 않는다. 연락처 검색은 초성·전화번호·띄어쓰기 무시를 지원한다(`searchContacts`).
- **긴 목록은 검색 정보를 미리 계산하고 100줄씩 그린다.** 연락처 수천 명을 한 번에 그렸더니 휴대폰에서 목록 표시에 5초가 걸렸다. 개선 후 0.7초.
- **Playwright로 네트워크를 가짜 응답으로 바꿀 때는 `serviceWorkers: 'block'`을 준다.** 서비스워커가 요청을 가로채서 `page.route`가 동작하지 않는다.
- **날짜 기록(`users/{uid}/days`) 동기화 구조 (2026-09-25, v585~v590).** 로그인 병합에서 같은 ID 할일을 무조건 로컬 우선으로 두면, 다른 기기 입력분이 이 기기의 옛 버전에 가려지고 서버에서도 지워진다. 지금 구조:
  - `persistDayData`는 저장 전 날짜를 "미동기화"(`dm_unsynced_days`)로 표시하고, 서버 저장이 성공하면 지운다. 로그인·새로고침 병합에서 **미동기화 날짜만 로컬 우선**, 나머지는 서버 우선이다. 로컬 우선은 할일만이 아니라 **메모·일기 등 날짜 전체**에 적용한다(`keepUnsyncedLocal`, 서버에만 있는 메모는 함께 살림) — 할일만 지켰다가 데스크탑 간편 메모가 만들자마자 사라진 적이 있다.
  - 로그인 시에는 서버 내용과 달라진 날짜만 다시 저장한다(예전엔 앱을 켤 때마다 전체 날짜를 다시 썼다).
  - 날짜 문서에는 서버 시각 `_syncedAt`을 기록한다(앱 `firebase.js saveDay`와 텔레그램 봇 모두). 앱 복귀·창 포커스 시 `loadDaysChangedSince`로 바뀐 날짜만 받는다(15초 간격 제한). 서버에서 날짜 문서를 쓰는 새 코드를 만들면 `_syncedAt`도 같이 써야 다른 기기에 반영된다.
- **데스크탑 포스트잇(간편 메모) 구조 (2026-09-25, 웹 v593 / 데스크탑 1.2.0).** 포스트잇 창은 앱 전체가 아니라 `?view=sticky` 가벼운 화면(`StickyMemo.jsx`, `main.jsx`에서 분기)이다. 포스트잇은 localStorage에만 쓰고 "미동기화" 표시만 하며, 트레이의 메인 창이 `storage` 이벤트로 받아 서버에 올린다(`App.jsx` onStorage). 창마다 앱 전체를 띄우면 창마다 로그인·전체 기록 읽기가 일어나므로 이 구조를 유지한다. 웹↔데스크탑 기능 호출은 `desktop/preload.js`의 `window.daymateDesktop`으로만 한다. `desktop/main.js`를 바꾸면 **설치 파일을 다시 만들어(`desktop`에서 `npm run build`) 사용자가 재설치**해야 반영된다(웹 배포만으로는 안 됨). 로컬 테스트는 `DAYMATE_URL`(로컬 서버)·`DAYMATE_USER_DATA`(별도 설정 폴더) 환경변수로 설치된 앱과 분리해서 Playwright `_electron`으로 한다. 데스크탑 단축키의 화면 이동은 버튼 글자를 찾아 누르지 말고 `dm:navigate`(화면 id)·`dm:open-search` 이벤트로 보낸다 — 글자 찾기 방식은 아래쪽 탭이 없는 화면(메모 관리자)에서 이동이 안 되는 문제가 있었다. 단축키 7종: 새 메모·간편 메모·메모 관리자·달력 보기·메모 검색·포스트잇 모두 보이기/감추기·최근 편집한 메모 열기(설정에서 지우기 가능).
- **메모 휴지통 (2026-09-25).** 메모가 날짜 기록에서 사라지면 `App.jsx`의 `stampMemoUpdates`(setDayData/setTodayData 공통 경로)가 `day.memoTrash`로 옮기고 30일 뒤 비운다. 그래서 메모 삭제 화면을 새로 만들 때 따로 휴지통 처리를 하지 않아도 되지만, **이 경로를 거치지 않는 저장(데스크탑 포스트잇처럼 localStorage 직접 저장)은 직접 memoTrash에 넣어야 한다.** 메모 삭제 때 사진 파일(Storage)을 지우지 않는다 — 영구 삭제(메모 관리자 휴지통) 때 지운다.
- **메모 편집 도우미는 `utils/editorAssist.js` 한 곳**(계산·날짜 넣기·줄 복제·목록 이어쓰기). 새 메모 입력칸을 만들면 `onKeyDown`에 `handleEditorKey`를 연결한다. 계산은 `eval`을 쓰지 않는 자체 계산기다.
- **반복 할일 규칙은 `utils/recurring.js`의 `matchesRecurring`** 한 곳에서 판단한다(`daily`, 요일 `0~6`, `nth:N:W` 매월 N번째 W요일).
- **PowerShell here-string 커밋 메시지에 큰따옴표가 있으면 git 인자가 깨진다.** 스크래치패드에 메시지 파일을 쓰고 `git commit -F 파일`로 커밋한다.
